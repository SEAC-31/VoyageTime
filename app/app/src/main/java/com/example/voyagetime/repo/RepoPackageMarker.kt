package com.example.voyagetime.repo

/**
 * Sprint 04 requires a visible repo package. The working repository contracts live in domain.repository and implementations in data.repository; this marker documents the architecture mapping without moving stable code.
 */
internal object RepoPackageMarker
